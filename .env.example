# Copy to `.env` in the repo root (gitignored). Scripts call `load_dotenv_optional()` so
# OpenAlex and other tools pick these up without exporting in the shell.
#
# OPENALEX_MAILTO=your-email@example.com
# OPENALEX_API_KEY=
# OPENALEX_KEY=
