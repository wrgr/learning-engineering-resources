/**
 * Shared user-facing strings for the hero, edition switcher, and footer.
 * Two static entry points: `index.html` (curated) and `merged.html` (broadened corpus).
 */
export const SITE_DISCLAIMER =
  "Errors and omissions in people, programs, and papers are unintentional. We are actively developing this site — ideas and contributions are most welcome.";

/** Short label for the default site (`index.html`, classic graph and paper set). */
export const EDITION_LABEL_CLASSIC = "Curated edition";

/** Short label for `merged.html` (extra broad-lane papers, Core / Expanded tiers). */
export const EDITION_LABEL_MERGED = "Broadened corpus";

/** Accessible description for the link from broadened → curated. */
export const EDITION_LINK_TO_CLASSIC_TITLE = "Open the curated edition (index.html)";

/** Accessible description for the link from curated → broadened. */
export const EDITION_LINK_TO_MERGED_TITLE = "Open the broadened corpus edition (merged.html)";
